- name: Clone repository
  hosts: web
  become: yes

  tasks:
    - name: Clone repository
      git:
        repo: "{{ repo_url }}"
        dest: /home/ec2-user/aws-study
        version: main
        force: yes 